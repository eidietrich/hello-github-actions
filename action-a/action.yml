name: "Hello Actions"
description: "Greet someone"
author: "eidietrich"

inputs:
  MY_NAME:
    description: "Who to greeet"
    required: true
    default: "World"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
